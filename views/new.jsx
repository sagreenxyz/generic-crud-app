const React = require('react');
const Default = require('./layouts/Default');

function New() {
    return (
        <Default>
            <h2>Add a New Item</h2>`
        </Default>
    )
}

module.exports = New;
